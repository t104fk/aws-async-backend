# AWS Async Backend and Stable Diffusion in Replicate

## Deploy

```
npm run cdk -- deploy AwsAsyncBackendStack --parameters replicateVersion=${model version} --parameters replicateToken=${replicate token} --require-approval never
```
